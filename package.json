{
  "name": "@webtex/root",
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "structs/*"
  ],
  "scripts": {
    "client:add": "pnpm -F @webtex/client install",
    "server:add": "pnpm -F @webtex/server install",
    "client:dev": "pnpm -F @webtex/client run dev",
    "server:dev": "pnpm -F @webtex/server run dev",
    "dev": "pnpm -r -run dev"
  },
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.16.6",
    "npm-dts": "^1.3.12",
    "tsconfig-paths": "^4.1.1",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "concurrently": "^7.6.0",
    "typescript-plugin-css-modules": "^4.1.1"
  }
}
